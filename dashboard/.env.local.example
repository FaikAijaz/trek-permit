# The backend API's address. Unlike the mobile app, this is a browser app —
# "localhost" works fine here as long as the dashboard and backend run on
# the same machine (they don't need to be on the same Wi-Fi network the way
# a phone does).
NEXT_PUBLIC_API_URL=http://localhost:3000
