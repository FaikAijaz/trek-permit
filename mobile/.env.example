# The backend's LAN address, reachable from a phone running Expo Go on the
# same Wi-Fi network. "localhost" won't work here — that would mean the
# phone itself, not this computer. Find yours with `ipconfig` (Windows) and
# look for the IPv4 Address under your active adapter.
EXPO_PUBLIC_API_URL=http://192.168.1.100:3000
